pipeline {
  agent any
  tools { 
        maven 'Maven_3.8.4'  
    }
   stages{
    stage('CompileandRunSonarAnalysis') {
            steps {	
		sh 'mvn clean verify sonar:sonar -Dsonar.projectKey=easybuggy-vulnerable-application -Dsonar.organization=anthonyjssrkey -Dsonar.host.url=https://sonarcloud.io -Dsonar.token='
			}
        } 
  }
}
